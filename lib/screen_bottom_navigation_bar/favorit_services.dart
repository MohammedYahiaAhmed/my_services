import 'package:flutter/material.dart';

class FavoritServices extends StatelessWidget {
  const FavoritServices({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text(
      'الخدمات المفضله',
      style: TextStyle(fontSize: 50),
    ));
  }
}
